#!/usr/bin/env perl6

use lib 'lib';
use JSON::Walker;

my $result = parse-json('test.json'.IO);
use Data::Dump;
print Dump $result;

$result = parse-json('[ "hello", 0 ]');
'10'.say;
$result.say;

LEAVE {
  try '#inst'.IO.rm;
}

# vi:syntax=perl6
